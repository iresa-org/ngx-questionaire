import { QuestionaireTemplateDirective } from './questionaire-template.directive';

describe('QuestionaireTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new QuestionaireTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
